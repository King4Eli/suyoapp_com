# 15JUN2026 21:42
0=IGNORE
1=fix now
x=completed

datingapp-rn-frontend/android/app/src/main/res
    Verify app icon, splash screen, package name, permissions, store metadata, and version numbers.

# DO NOW
- [1] Add clear failed-payment and cancelled-payment states.
- [1] Add restore purchases / refresh entitlement action.
- [1] Add account deletion confirmation and backend deletion/anonymization workflow.
- [1] Match updates check only match_id, never that the caller is a participant.
    ,Any user who guesses or enumerates a match ID can alter or unblock someone else's match record.
    ,CHECKED 2026-07-15: pushPeopleToMatch.js's UPDATE already has `AND (match_user_id_from = ? OR match_user_id_to = ?)` bound to the caller -- this one looks already fixed in current code, unlike the two above. Leaving noted in case it regresses.

# Move IGNORE HERE
- [0] restore-purchases action that works
- [0] Remove production `console.log` statements from discovery.
- [0] Implement voice call and video call .
- [0] Confirm logout clears profile cache, products cache, socket state, and navigation history.
- [0] add skeleton for pages that requires api, like likes.tsx profile.tsx instead of lottie files and a small obvious loader
- [0] Rate-limit / abuse protection on report-user and block, similar to what OTP endpoints already have --
    ,currently unlimited, unlike every auth-adjacent endpoint in the codebase.
- [0] Voice/video call: explicitly filed under todo.txt's IGNORE section already (deliberately deprioritized) --
    ,noting it exists as an idea, not recommending it move up.
- [0] Session revocation ("log out all devices", force-logout on password/phone change): flagged as a gap during
    ,the JWT migration this session -- current tokens are self-contained and can't be invalidated before they
    ,naturally expire. Worth it once you have real reports of stolen/leaked tokens, not urgent otherwise.
- [0] In-app photo/identity verification (selfie-vs-profile-photo check) -- user_verified exists as a column and
    ,is surfaced everywhere (badges, filters) but nothing in the app can currently set it except a manual/admin path.
- [0] Fix the streak feature end-to-end (above) -- infra (rose balance, transactional patterns) already exists,
    ,this is mostly wiring, not new architecture.
- [0] Streak count is fake end-to-end: backend (getProfile.js) computes it as Number(user_last_accessed), which
    ,is a timestamp, not a day-count -- meaningless. Frontend (Profile.tsx) reads a field
    ,(profile.user_effect.streakcount) that doesn't exist anywhere in the real API response, so it silently
    ,always renders 1 dot lit regardless of actual usage. See conversation for a concrete implementation plan
    ,(new user_streaks table, tick on getProfile, award 1 rose every 7-day streak via the existing
    ,user_rose_usage.rose_balance column) -- not yet built, pending go-ahead.






# Move COMPLETED HERE
- [x] Add resend action for failed messages, images or media. FIXED: Conversations.tsx no longer deletes a
    ,failed optimistic message -- it's kept on screen with status:'failed' and a "tap to retry" row (a failed
    ,media message still holds its local file:// uri in `src`, so retry re-uploads from the same file). Each
    ,bubble now sends/retries independently (attemptSendText/attemptSendMedia via Promise.allSettled) instead
    ,of one all-or-nothing batch, so one failed image doesn't take a successfully-sent text message down with it.
- [x] Read receipts / typing indicators. read receipt for vip. FIXED, both halves:
    ,Read receipts: new users.user_privacy_read_receipts column (migration in
    ,admin/global/_sql/_migrations/2026-07-15_add_read_receipts_privacy.sql -- run it against the live DB, this
    ,repo has no migration runner). Mutual/WhatsApp-style: getConversation.js only attaches `read` on a message
    ,you sent if YOU are VIP, YOU have the setting on, AND the recipient has it on too -- pushProfile.js
    ,re-checks VIP server-side before honoring an attempt to turn it on (the Settings.tsx toggle hiding it from
    ,non-VIP users isn't an auth boundary by itself). Wired into Settings.tsx's Privacy sheet -- this is also
    ,the first real user of ModernSwitch's premiumLock prop, which existed unused before this. Also fixed a
    ,real bug found along the way: getConversation.js only ever marked the LAST message read, so older
    ,unread messages in a burst stayed "unread" forever -- now marks the whole not-from-me backlog.
    ,Live updates via a new 'messages-read' socket event (thin ping, no payload beyond matchId, so it can't
    ,leak receipt data to someone not entitled to see it -- the real `read` value only ever comes from the
    ,privacy-gated getConversation response).
    ,Typing indicators: NOT tier-gated (only read receipts said "for vip"). New 'typing'/'stop-typing' socket
    ,events in realtimedata.js, gated on already having joined the match room (join-match-room already verified
    ,participant status, so this can't be used to spam an arbitrary match). Shows "typing..." in Conversations.tsx
    ,in place of the city/bio-hint line.
- [x] add your-strong-secret to .env. FIXED (and went further): the AES key in tools (global/functions.js) was `crypto.scryptSync('your-strong-secret', 'salt', 32)` -- a literal string in source, so anyone with repo access could derive it and decrypt/forge session tokens (session hijacking). Also found+fixed: the IV was a static class field generated ONCE at process start and reused for every token ever issued (CBC requires a fresh IV per encryption). Now: key is derived from SESSION_ENCRYPT_HASH (already a required env var) with a domain-separating salt, IV is fresh crypto.randomBytes(16) per encodeStr() call, and the auth_hash comparison in verifyFullSession uses crypto.timingSafeEqual instead of `!==`. Verified: legit round-trip still works, two tokens now get different IVs, and a token forged with the old hardcoded key is rejected.
- [x] Any authenticated user can fetch another user's full row by ID. FIXED: getPeopleToMatch.js now selects an explicit safe-column allowlist (CANDIDATE_PROFILE_COLUMNS) instead of SELECT *, for both the getOnePersons_id2 lookup and the main discovery query.
- [x] sessions.currentUserID was a process-wide static field (global/functions.js), not per-request -- concurrent requests interleaving at `await` could read/write each other's user id, leaking profiles/likes/conversations/payments across users. FIXED: backed by AsyncLocalStorage, wrapped per-request in index.js and per-socket-handshake in realtimedata.js.
- [x] Remove blur from chats.tsx if subscription. FIXED: the "See likes" teaser image in renderConnectionsSection used a hardcoded blurRadius; now `activeSubscription ? 0 : (Platform.OS === "android" ? 60 : 30)`, and activeSubscription was added to that useCallback's dependency array so it un-blurs immediately after purchase instead of waiting on some other dependency to change.

